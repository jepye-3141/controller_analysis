# Ballistic-Chain Bugsweep — 2026-07-10 (ultracode)

**Scope:** final pre-regeneration sweep of the whole ballistic chain — `eom2.m` (physics + numerics), `aero_constants.m`, `Mortar_Sim.m`, `ballistic_deploy_state.m`, `ballistic_success.m`, `sweep_landing_centroid.m`, the ballistic sections of `analysis.m`, and cross-file contracts (`j3_lut_regen.m`, `trajectory_optimization.m`, replot/surrogate touchpoints).

**Method:** 7 scoped finder agents -> dedup/merge -> adversarial 3-lens verification per finding (trace/repro, documented-intent, real-impact; low-severity findings 1 lens) -> completeness critic -> gap round. Static review only (no MATLAB launched; python3 for arithmetic). Workflow run `wf_a29859aa-4d4`: 44 agents, 43 completed, ~4.01M subagent tokens, 584 tool uses, 87 min wall.

**Outcome:** 26 raw findings -> 16 unique verified -> **19 CONFIRMED** (5 high / 6 medium / 8 low; findings 16-19 from the gap-round addendum below), 1 refuted, 0 merely-plausible. The physics finder validated its reading of eom2 by transcribing the full RHS into a pure-Python RK4 and reproducing the shipped `figs/0_ballistic_trajectory.png` (apogee ~391 m, impact ~(867, 377) m at 20.1 s, apogee h·r = -0.807) to ~4 digits before testing corrections against it.

**Bottom line: the regeneration campaign is BLOCKED.** Findings 1-4 mean every trajectory ever produced by this chain was flown on a first-order-wrong ballistic model (the canonical 391 m apogee exceeds the ~240-255 m vacuum ceiling for a 100 m/s, 45 deg launch — physically impossible with drag and no thrust), and every deploy seed hands the drone a mirror-image tumble. Fix, re-verify, then regenerate once.

---

## 1. [HIGH] Ballistics_Simulation-master/eom2.m:261 — correctness

**Lift-force tilde coefficient carries an extra factor v_mag, making the lift acceleration ~V times (70-100x) too strong versus McCoy eq. 9.1 and dimensionally inhomogeneous, so every trajectory flies a lift-lofted arc that is physically impossible.**

*Evidence:* eom2.m:261 `C_tilde_L_a = rho*v_mag*S*C_L_a / (2*m);` is multiplied at lines 288-289/294-295/300-301 by the McCoy bracket `(v_mag_sq*r(1) - v_mag*v(1)*cos_taoa)` = [V^2 x - (V.x)V], which already carries V^2 -- the correct tilde coefficient is rho*S*C_L_a/(2*m). Structural cross-check in-file: drag (line 260) correctly folds v_mag because its multiplier v(1) is only V^1-scale, and the overturning moment (line 266) folds v_mag against a V^1-scale (v x r) bracket -- the lift line is the odd one out. Finder's pure-Python RK4 transcription of the full RHS (tables from the xlsx) reproduces the shipped figs/0_ballistic_trajectory.png and the documented apogee h.r = -0.807 to 4 digits, confirming the shipped sim flies the inflated-lift arc: apogee 391 m at ~10 s, impact (867, 377) m at 20.1 s -- 65% ABOVE the vacuum apogee (~238 m) despite drag, impossible for a 13.6 kg mortar bomb at alpha <= 4.6 deg; with the extra v_mag removed the arc becomes consistent (apogee 234 m, flight 13.85 s vs vacuum limit 13.97 s, impact (909, 348) m). The fig's v_west subplot shows the tell-tale nonphysical ~20 m/s lateral velocity kick in the first second (aerodynamic jump amplified ~100x).

*Failure scenario:* Canonical launch (Vo=100, el=45, az=15, w_z0=1, w_y0=0.5, alpha_0=2, beta_0=-0.5, p=-8.379): every quantity the swarm pipeline consumes -- deploy-station positions/velocities/pointing along the 20-80% arc, apogee state, impact point, arc length, flight time -- is computed on an arc ~157 m too high, ~6.2 s too long, and ~51 m displaced at impact. At the LUT box corner (Vo=120, el=55, az=30, w_z0=2, w_y0=1, p=-12) correcting the term shifts the impact point by (+130, +276) m, ~3x the GP LOO-RMSE, so the pending regeneration campaign (N=41 LUT, GP theta*, sat sweeps, envelope/fig 39) would bake a first-order wrong ballistic model into all results and paper numbers.

*Verification:* 3 lens(es): REAL, REAL, REAL
  - lens 1 (high): CONFIRMED by full independent reproduction. (1) Code verbatim-real: eom2.m:261 'C_tilde_L_a = rho*v_mag*S*C_L_a / (2*m);' multiplied at 288-289/294-295/300-301 by '(v_mag_sq*r(i) - v_mag*v(i)*cos_taoa)'; since cos_taoa=(v.r)/v_mag (line 233) the bracket IS McCoy's [V^2 x - (V.x)V] (already V^2-scale), and no compensating /v_mag exists anywhere in the dv block. (2) Dimensional analysis is airtight: shipped term = [1/s]*[m^2/s^2] = m^2/s^3, not acceleration; correct lift (rho*S*C_L_a/2m)[V^2 x-(V.x)V] gives m/s^2. Audit of every tilde [...]
  - lens 2 (high): NOT a documented decision, and the finding is independently confirmed. (1) Documentation contract: every statement of intent declares McCoy Ch.9 fidelity — Mortar_Sim.m:6-7 ("equations of motion ... from Modern Exterior Ballistics, 2nd edition, McCoy, Chapter 9"), upstream Ballistics_Simulation-master/README.md:11, docs/BALLISTICS_REFERENCE.md:5 ("Based on McCoy ... Ch. 9"; it documents coefficient composition C_L_a = C_L_a0 + C_L_a2*sin^2(alpha) but never the force-term scaling), and CLAUDE.md's overview. No document mentions any [...]
  - lens 3 (high): TECHNICAL CONFIRMATION (spot-checked, not just assumed): eom2.m:261 reads `C_tilde_L_a = rho*v_mag*S*C_L_a / (2*m);` and multiplies the bracket `(v_mag_sq*r(i) - v_mag*v(i)*cos_taoa)` at lines 288-289/294-295/300-301. Since v_mag*cos_taoa = V·r, the bracket is exactly McCoy eq 9.1's [V²x − (V·x)V], already V²-scale. Dimensional check (python): coded coefficient (1/m)(m/s)=1/s times bracket m²/s² gives m²/s³ — not an acceleration; the correct rho*S*C_L_a/(2m) (1/m) gives m/s². In-file structural cross-check agrees: drag (line 260) folds [...]

## 2. [HIGH] ballistic_deploy_state.m:53 — units-frames

**rotvel0 = M.'*h_nwu omits the det(M) = -1 factor required when transforming an angular-velocity pseudovector through the plant's deliberately improper body-to-world map, so every deploy seed hands the drone the exact mirror of the shell's tumble (realized world angular velocity -h instead of +h); the consistent seed is rotvel0 = -(M.'*h_nwu).**

*Evidence:* ballistic_deploy_state.m:52-53: `vel0 = M.' * v_nwu; % world -> body` / `rotvel0 = M.' * h_nwu;` with M (lines 48-50) orthogonal, det = -1 (docstring line 28 states it). The plant's attitude kinematics (system_dynamics.m:46-48) are the standard proper-ZYX Euler-rate relations, so M evolves as Mdot = M*skew(w); conjugating, Mdot*M.' = det(M)*skew(M*w) = -skew(M*w), i.e. the seeded body triad's observable world angular velocity is -M*w; with w = M.'*h that is -h. Shell ground truth is unambiguous: eom2.m:326-328 integrates dr = h x r (right-handed; the NUE->NWU map at eom2.m:134-137 is proper, det = +1). Four finders verified this independently and it was re-verified during this merge (pure-Python central-difference of the plant's own Euler-rate equations with the coded M, 300 random attitudes): max ||Mdot*M.' - skew(+M*w)|| = 6.30 (O(1) wrong) vs ||Mdot*M.' - skew(-M*w)|| = 3.4e-10; at deploy attitudes (phi=0, psi=atan2(r2,r1), theta=asin(r3)) the code seed gives nose rate d/dt M(:,1) = -(h x r) exactly (negated seed matches h x r to 2.4e-10); axial case h = -0.807*r_hat: seeded wx = -0.807 makes d/dt M(:,2) = +0.807*(r x M(:,2)), opposite the shell. Velocity/position/attitude seeds are exact (proper vectors: M*M.'*v = v). The author's B4 verification checks are all sign-blind (axial spin lands on the body roll axis, |omega| = 0.825, nose_err), so this passed them; after the fix the roll-axis check would read +0.807. Callers hit on every trial: sweep_landing_centroid.m:174 (per-station), analysis.m:251 (apogee test) and :294 (envelope loop).

*Failure scenario:* At the sat_on operating point (p = -8.379, apogee h.r = -0.807, transverse ~0.17-0.19 rad/s) every one of the ~140 sweep trials, the apogee test, and all 21 envelope trials start the drone tumbling exactly opposite the shell it just separated from: roll sense about the nose and the transverse nose swing are both reversed, so arrest transients, Euler-rate excursions near the theta -> +/-90 deg stations, rotvel traces, and landing points differ from the physical hand-off in all trials. CLAUDE.md records that a ~0.4 m seed re-basing flipped one criterion-marginal trial; a globally mirrored 0.83 rad/s initial tumble is a far larger IC perturbation (Jxx = Jyy gives only partial mirror symmetry; cross-track/yaw asymmetries do not cancel), so the pending sat/LUT/SO/envelope regeneration would canonize mirror-tumble reachability/centroid/theta* numbers as the physically-seeded results. If the accepted axial I_x/I_y approximation is ever removed (true |p| ~ 8 rad/s), the same sign error mirrors an 8 rad/s roll. Fix is one negation on line 53 (line 52's velocity map stays as-is).

*Verification:* 3 lens(es): REAL, REAL, REAL
  - lens 1 (high): CONFIRMED by independent trace and pure-Python reproduction. (1) Code is as quoted: ballistic_deploy_state.m:53 `rotvel0 = M.' * h_nwu;`, with M (lines 48-50) orthogonal, det = -1 (verified numerically), M(:,1)=r_hat at deploy attitude to 6.6e-16. (2) M is the plant's true body->world map: its rows match system_dynamics.m:49-55 xdot(10:12) term-for-term and the gravity rows xdot(1:3) equal M.'(-g*zhat). (3) Decisive kinematic test: transcribing the plant's exact Euler-rate equations (system_dynamics.m:46-48, standard proper-ZYX formulas) [...]
  - lens 2 (high): Documentation lens verdict: NOT a documented decision, and the finding holds on the merits. (1) Documentation search: ballistic_deploy_state.m's docstring (lines 18-34) documents the phi=0 packaging convention, M's construction, orthogonality, and det = -1, and explicitly flags exactly one accepted approximation on h ('axial I_x/I_y understatement is the accepted approximation') — but line 53 carries no comment and nothing addresses the pseudovector/det(M) factor or rotation sense. CLAUDE.md (both required sections) says 'velocities/rates [...]
  - lens 3 (high): Reachability is total: the flagged line (ballistic_deploy_state.m:53) is on the seed path of every trial of every campaign driver. sweep_landing_centroid.m:174 feeds it interp'd trajectory cols 4:6 (= h) for all 8 stations -> ~140 trials/sweep, called by trajectory_optimization.m (both arms; p=-8.379, w_z0=1, w_y0=0.5 at lines 11-23, giving the documented deploy |omega| ~ 0.825 rad/s, h.r = -0.807), by j3_lut_regen.m (41 samples over w_z0 in [-1,2], w_y0 in [-1,1], p in [-12,12] at lines 37-43 -- zero deploy tumble is measure-zero, and [...]

## 3. [HIGH] Ballistics_Simulation-master/eom2.m:282 — units-frames

**The Coriolis lambda terms apply radian sin/cos to the degree-valued az_0 (and latent env.L) AND misuse the firing azimuth as McCoy's frame azimuth in a north-fixed frame (double-counting the azimuth already embedded in v0/r0), making the pseudo-force wrong in every run -- worse than omitting Coriolis entirely -- and az-correlated across the LUT box.**

*Evidence:* eom2.m:282-284: `lambda(1) = 2*omega*(-v(2)*cos(env.L)*sin(az_0) - v(3)*sin(env.L)); lambda(2) = 2*omega*(v(1)*cos(env.L)*sin(az_0) + v(3)*cos(env.L)*cos(az_0)); lambda(3) = 2*omega*(v(1)*sin(env.L) - v(2)*cos(env.L)*cos(az_0));`, feeding dv at lines 292/298/304. az_0 = launch.az (line 8) is degrees everywhere else in the same file (lines 19-25, 39-46 all use sind/cosd, e.g. `Vo*cosd(el_0)*cosd(az_0)`); verified numerically: sin(15 rad) = +0.6503 vs sind(15) = +0.2588, cos(15 rad) = -0.7597 vs cosd(15) = +0.9659 (sign flip), and at the LUT az box edge sin(30 rad) = -0.9880 (wrong sign, near-full magnitude). env.L carries the same trap (aero_constants.m:6 documents it as 'latitude of firing site, degrees'; gravitywgs84 at aero_constants.m:8 consumes it as degrees) -- currently inert only because L = 0. Independently, McCoy's Coriolis formulas take AZ = azimuth of the frame +x axis; this frame is north-fixed (Mortar_Sim.m:5 'assume range is oriented northwards', with az already encoded in the v0/r0 components), so AZ should be 0 -- passing the firing azimuth double-counts it even after a sind/cosd fix. Finder replica quantification at the operating point: correcting the term moves the impact point by ~(+0.87, -0.68) m (~0.7 m horizontal, from integrating the 2*omega error acceleration over the ~20 s flight), while omitting Coriolis entirely differs from correct by only ~(+0.46, -0.31) m.

*Failure scenario:* Every current run (launch.az = 15 in Mortar_Sim.m, both analysis.m launches, trajectory_optimization.m; LUT box az in [0,30]) carries a ~0.5-0.7 m impact/arc bias -- larger than the 0.43 m true-position error the 2026-07-10 tolerance-tightening campaign was executed to remove (RelTol 1e-8 'converged to ~2e-6 m' of an ODE wrong by ~0.7 m). The documented criterion sensitivity (a ~0.4 m re-basing flipped one of 140 sweep trials) means this bias can flip trials in the pending regeneration, and because the error varies erratically with az (zero at az=0, wrong-signed near az=30), it injects a spurious az trend into deploy seeds, centroids, and GP lookup-table training data.

*Verification:* 3 lens(es): REAL, REAL, REAL
  - lens 1 (high): Verified end-to-end; every element of the finding is real. (1) Quoted code real: eom2.m:282-284 verbatim `lambda(1) = 2*omega*(-v(2)*cos(env.L)*sin(az_0) - v(3)*sin(env.L)); lambda(2) = 2*omega*(v(1)*cos(env.L)*sin(az_0) + v(3)*cos(env.L)*cos(az_0)); lambda(3) = 2*omega*(v(1)*sin(env.L) - v(2)*cos(env.L)*cos(az_0));` feeding dv at 292/298/304, active since env.omega_earth=7.292e-5 (aero_constants.m:10). (2) Degrees-as-radians confirmed: az_0 = launch.az (eom2.m:8) is degrees in every caller (Mortar_Sim.m:17, analysis.m:228/270, [...]
  - lens 2 (high): Not a documented decision, and the finding holds on the merits. Documentation search was exhaustive: (1) eom2.m's lambda block (lines 282-284) has zero comments, in a file where every deliberate deviation carries a dated in-file author-decision note (o-channel removal L56-62, tolerances L74-79, h0 McCoy eq. 9.4 fix L48-49); (2) CLAUDE.md's Ballistic-simulation and Helper-scripts sections never mention Coriolis; (3) docs/BALLISTICS_REFERENCE.md:71-72 and docs/README.md:337 affirmatively document the intent as modeling the physical [...]
  - lens 3 (high): Reachable in every configuration the drivers actually run, and material at the project's own documented sensitivity threshold. Reachability: eom2.m:282-284 applies radian sin/cos to degree-valued az_0 (the same file uses sind/cosd on az_0 at lines 19-46), lambda feeds dv unconditionally at lines 292/298/304, and omega_earth=7.292e-5 (aero_constants.m:10) is never zeroed. Verified call sites: trajectory_optimization.m:14 az=15 (via sweep_landing_centroid.m:80), analysis.m:228 and 270 az=15, Mortar_Sim.m:17 az=15, j3_lut_regen.m:40 LHS box [...]

## 4. [HIGH] Ballistics_Simulation-master/eom2.m:262 — correctness

**The Magnus-force tilde coefficient omits the reference area S, making the Magnus force 1/S ~ 89x too strong and dimensionally wrong -- a systematic spin-correlated distortion of every trajectory with nonzero p.**

*Evidence:* eom2.m:262: `C_tilde_N_pa = rho*d*C_N_pa*p / (2*m);` -- units (kg/m^3 * m * 1/s / kg) times the V-scale (r x v) bracket at lines 290/296/302 do not yield acceleration. McCoy's Magnus force is (1/2)*rho*V^2*S*(p*d/V)*C_Npa*sin(alpha)/m, so the tilde coefficient must be rho*S*d*C_Npa*p/(2*m). The sibling Magnus MOMENT at line 267 correctly includes S (`C_tilde_M_pa = rho*S*(d^2)*C_M_pa*p / (2*I_y);`), proving a transcription slip -- this is the only one of the nine tilde coefficients lacking S. Finder replica quantification at the operating point: restoring S moves the impact point by (+1.76, -1.46) m, i.e. the shipped code carries a ~2.3 m spurious Magnus displacement at p = -8.4.

*Failure scenario:* Any launch with nonzero spin (operating p = -8.379; LUT box p in [-12,12]): range/crossrange carry a spurious Magnus-force displacement of ~2.3 m at p = -8.4, scaling linearly with |p| (~3-4 m at the p = +/-12 box edges) and varying in sign with p across the LHS samples -- a systematic, spin-correlated distortion of the LUT/GP training data roughly 5x larger than the 0.43 m true-position error the 2026-07-10 tolerance tightening was executed to remove.

*Verification:* 3 lens(es): REAL, REAL, REAL
  - lens 1 (medium): CONFIRMED by code read + independent numerical reproduction. (1) Quoted code is real: eom2.m:262 `C_tilde_N_pa = rho*d*C_N_pa*p / (2*m);` is the only one of the nine tilde coefficients (lines 260-268) lacking S; it multiplies (r x v) brackets at lines 290/296/302, while the sibling Magnus moment at line 267 includes S (`rho*S*(d^2)*C_M_pa*p / (2*I_y)`). (2) Dimensional proof: as coded, coefficient units are (kg/m^3 * m * 1/s)/kg = m^-2 s^-1; times the (r x v) bracket (m/s) gives m^-1 s^-2, not acceleration - the gap is exactly S = [...]
  - lens 2 (medium): Not a documented decision — documentation actively contradicts the code. I checked every venue the intent could live in: CLAUDE.md's 'Ballistic simulation' and 'Helper / utility scripts' sections (document the h0 fix, deploy-attitude fix, 12-state reduction, and tolerance tightening in detail; zero mention of Magnus or any aero-coefficient choice), eom2.m's own comments (the tilde block at lines 256-268 is uncommented; header comments cover state layout/h0/tolerances only), docs/BALLISTICS_REFERENCE.md (frames the model as 'Based on McCoy [...]
  - lens 3 (high): Reachable in 100% of the regeneration campaign's configurations and material at the precision the project quotes. Code evidence verified verbatim: eom2.m:262 `C_tilde_N_pa = rho*d*C_N_pa*p / (2*m)` is the only one of nine tilde coefficients (lines 260-268) lacking S, while the sibling Magnus moment (line 267) includes it; aero_constants.m:37/44 loads C_N_pa as a plain dimensionless McCoy table coefficient (no S pre-folded), and docs/BALLISTICS_REFERENCE.md documents it as a standard table — nothing anywhere documents the omission as [...]

## 5. [MEDIUM] j3_lut_regen.m:76 — robustness

**The checkpoint resume path validates only N_LHS/field_names/ranges -- no ballistics/criterion fingerprint -- so the completed pre-deploy-fix checkpoint still on disk makes a post-fix 'regeneration' re-launch silently skip all 41 simulations and re-stamp seeding-stale data as the canonical LUT log (and a partial stale checkpoint would resume into a mixed-physics log).**

*Evidence:* j3_lut_regen.m:76-89: `if isfile(ckpt_path) ... assert(ck.N_LHS == N_LHS && isequal(ck.field_names, field_names), ...)` plus per-field ranges asserts only -- none of these changed with the 2026-07-10 eom2 12-state/tolerance/deploy-attitude rework. Line 100 `if ~done(i_ref)` and line 123 `if done(n), continue; end` skip completed samples, so with the retained fully-done checkpoint (verified present: logs/centroid_lookup_ckpt_j3.mat with `done` 41x1 complete) every sample -- including the reference-row injection block and its now-invalid reach ~0.55 assert at lines 102-103 -- is bypassed; lines 148-152 then overwrite logs/centroid_lookup_log.mat and print the success summary with the loaded wall_s. CLAUDE.md's checkpoint bullet ('safe to delete now that the canonical log is verified, harmless to keep') is factually wrong post-fix: keeping it defeats the next regeneration.

*Failure scenario:* Operator re-launches j3_lut_regen (per its own header: 're-launch the same command to resume') to regenerate the LUT on the fixed ballistics: the config asserts pass, the console prints 'resuming from checkpoint: 41/41 samples complete' then 'wrote logs/centroid_lookup_log.mat: N=41 entries ... total sim wall 2.3 h', and the canonical log is rewritten with byte-identical pre-fix data -- zero re-simulation, no error. Alternate path: a run aborted at sample k before an eom2 change and resumed after it yields one canonical log whose rows were simulated under different physics, indistinguishable downstream (j3_surrogate_refit trains GPs on the mixture).

*Verification:* 3 lens(es): REAL, REAL, REAL

## 6. [MEDIUM] sweep_landing_centroid.m:38 — robustness

**The macOS license-contention guard is dead: BSD pgrep on this host does not support -c, so the probe always yields 0 (or usage-text -> NaN), n_matlab never exceeds 1, and the sweep launches alongside another MATLAB instead of aborting.**

*Evidence:* sweep_landing_centroid.m:38-46: `[~, n_str] = system("pgrep -c MATLAB_maca64 || echo 0"); n_matlab = str2double(strtrim(n_str)); if n_matlab > 1 ... error(...)`. Verified on this host (Darwin 25.5): `pgrep -c MATLAB_maca64` prints `usage: pgrep [-Lfilnoqvx] ...` to stderr and exits nonzero, so `|| echo 0` emits "0" -- n_matlab = 0. If MATLAB's system() merges stderr into cmdout, str2double gives NaN and `NaN > 1` is still false. Either way the guard fails open every run, contradicting its own comment block (lines 33-36 'refuse to run if another MATLAB session is open') and the documented abort behavior CLAUDE.md attributes to this function. A working count (e.g. `pgrep MATLAB_maca64 | wc -l`) always returns >= 1 (self), so n_matlab = 0 is itself proof the probe never worked.

*Failure scenario:* User leaves a GUI MATLAB open and launches the LUT regeneration headless (j3_lut_regen -> sweep_landing_centroid): the guard computes n_matlab = 0 and proceeds; parpool workers then collide on Parallel Computing Toolbox seats and die mid-run with the exact failure the guard exists to pre-empt ('worker shut down unexpectedly with status 1'), wasting hours of a checkpointed multi-hour campaign instead of erroring at t = 0.

*Verification:* 3 lens(es): REAL, REAL, REAL

## 7. [MEDIUM] CLAUDE.md:114 — doc-mismatch

**CLAUDE.md's claims that 'h = omega at deploy (the sweep's use of h as the drone's initial body omega is now physically correct)' and that h.r = -0.807 is 'the known axial spin' are factually wrong at the current operating point p = -8.379: h differs from omega by 9.93x on the axial component, and the code's own docstring downgrades this to an 'accepted approximation'.**

*Evidence:* CLAUDE.md:114: 'giving h.r ~ 0 for the fin-stabilized mortar, so h = omega at deploy (the sweep's use of h as the drone's initial body omega is now physically correct)' and 'apogee h.r = -0.807, drone seed |omega| = 0.825 rad/s' -- written for the p = 0 state and immediately falsified by the deliberate-spin sentence that follows; line 116: 'puts the known axial spin (h.r = -0.807) exactly on the body roll axis'. By line 114's own decomposition ('axial part is (I_x/I_y)*p') and aero_constants.m:15-16 (I_x = 0.02335, I_y = 0.23187, so I_y/I_x = 9.930), apogee h.r = -0.807 implies a true axial spin of ~-8.01 rad/s and true deploy |omega| ~ 8.02 rad/s, not 0.825. ballistic_deploy_state.m:7-9 states the accurate version: 'h ... (~ body omega for the fin-stabilized round; axial I_x/I_y understatement is the accepted approximation)'.

*Failure scenario:* The next consumer trusts 'physically correct' and validates the rotation criterion or the paper's deploy-tumble narrative against |omega| = 0.825 rad/s: the criterion bound 2*norm([pi pi pi]) = 10.88 rad/s appears to have 13x margin over the deploy tumble when the physical margin is only ~1.4x (8.0 vs 10.88), so a criterion-threshold or grace-window decision made from the doc silently fails to survive any future removal of the approximation, and any paper sentence quoting 0.825 rad/s as the physical deploy rotation rate is wrong by an order of magnitude.

*Verification:* 3 lens(es): REAL, REAL, REAL

## 8. [MEDIUM] CLAUDE.md:93 — doc-mismatch

**CLAUDE.md states the swarm models consume a 15-element xi, but every driver in the codebase builds and passes a 12-element xi and no 12-to-15 padding exists anywhere -- the documented interface contract is factually wrong and would mislead the next driver author.**

*Evidence:* CLAUDE.md:93: 'All swarm models consume workspace variables `xi` (15-element initial state)'. Reality: ballistic_deploy_state.m:55 returns 12 elements (`xi = [vel0; rotvel0; theta0; phi0; psi0; pos_nwu]`); sweep_landing_centroid.m:174 seeds parsim trials with that 12-vector unmodified; analysis.m:132/183/201 build `xi = [vel0; rotvel0; rot0; x0_step]` (3+3+3+3 = 12) for the 10-drone models and analysis.m:251/294 pass the helper's 12-vector to all four *_single models. These runs demonstrably complete (2026-07-03 and 2026-07-10 regenerations, 0 errored trials), which is impossible if any model indexed xi(13:15); no pad-to-15 step exists in any .m file (constants.m's `Ki_d = Kd(:, 13:15)` is the LQR gain partition, unrelated).

*Failure scenario:* A new sweep/driver author follows CLAUDE.md and constructs a 15-element xi (e.g. appending 3 LQR integral-state seeds): the models' 12-wide initial-state wiring errors at sim init across the whole parsim batch, or -- if a block slices xi(1:12) -- the extra states are silently ignored; conversely an auditor reading ballistic_deploy_state's 12-vector against the doc wrongly concludes the seed is missing three states and hunts for padding logic that does not exist.

*Verification:* 3 lens(es): REAL, REAL, REAL

## 9. [MEDIUM] Ballistics_Simulation-master/eom2.m:86 — doc-mismatch

**The exported .alpha/.beta fields are velocity direction-cosine angles from the up/east axes -- not the angle-of-attack and sideslip that the in-code comment and BALLISTICS_REFERENCE.md claim -- and the console 'Impact angle' rebuilt from them treats the two axis-angles as spherical coordinates, under-reporting the true descent angle by ~6 deg on the canonical shot.**

*Evidence:* eom2.m:85-87: `% Orientation angles (aoa and sideslip)` / `alpha = acosd(x(:,2)./(...));` / `beta = acosd(x(:,3)./(...));` -- acosd(v_up/|v|) and acosd(v_east/|v|), never involving the pointing vector r; the true total AoA (acos(cos_taoa)) exists only inside the RHS at line 234 and is never exported. Verified: at t = 0 (el=45, az=15) .alpha = 46.92 deg while the true AoA is ~2 deg. docs/BALLISTICS_REFERENCE.md:41 states '.alpha, .beta -- angle of attack and sideslip histories'. Downstream, lines 115-123 form `impactVect = [cosd(impact_beta)*cosd(impact_alpha), sind(impact_beta)*cosd(impact_alpha), sind(impact_alpha)]` and `impact_angle = acosd(dot(vert,impactVect)/...) - 90`, i.e. acosd(sind(beta)*cosd(alpha)) - 90, which does not invert the direction cosines and equals the true descent angle (exactly impact_alpha - 90) only when v_east = 0. Verified numerically: canonical replica impact velocity NUE [35.6, -81.3, 36.6] gives printed 51.5 deg vs true 57.9 deg (6.34 deg error); [...]

*Failure scenario:* The next consumer -- e.g. the planned W2/W3 LUT schema extension or anyone adding a deploy-AoA column -- greps ballistic_sol.alpha per the reference doc and gets 46.9 deg at launch instead of ~2 deg (and ~45-deg-scale garbage along the arc), silently corrupting any AoA-conditioned analysis; meanwhile the standalone entry point's quotable console 'Impact angle' is ~6 deg shallower than the true descent angle whenever crossrange velocity is nonzero (always, with az = 15).

*Verification:* 3 lens(es): REAL, REAL, REAL

## 10. [LOW] Ballistics_Simulation-master/aero_constants.m:44 — robustness

**The Magnus-coefficient scatteredInterpolants use the default unbounded linear extrapolation outside the (Mach, alpha^2) data hull, which tops out at alpha^2 = 1316 deg^2 (36.3 deg total AoA), so high-tumble LHS corner launches get fabricated C_N_pa/C_M_pa with no warning.**

*Evidence:* aero_constants.m:44-45: `env.interp_C_N_pa = scatteredInterpolant(env.lut_C_N_pa(:,1), env.lut_C_N_pa(:,2), env.lut_C_N_pa(:,3));` (and interp_C_M_pa) -- no Method/ExtrapolationMethod arguments, so ExtrapolationMethod defaults to 'linear'. Parsed from the xlsx: both tables span Mach 0-1.55 and alpha^2 0-1316 deg^2 (alpha <= 36.27 deg), and C_M_pa changes sign within the table (e.g. -0.46 to +2.35 along Mach 0.48). Queried at eom2.m:245/249 with `rad2deg(alpha).^2`, where alpha = acos(...) can reach 180 deg (alpha^2 up to 32400). By contrast, the 1-D Mach lookups are protected by the [...]

*Failure scenario:* An LHS corner sample (w_z0 near 2, w_y0 = +/-1, alpha_0 = 2, |p| toward 12, from the j3_lut_regen box) has a first-oscillation AoA amplitude of roughly (transverse rate ~2.5 rad/s)/(pitch frequency ~5.2 rad/s) ~ 28-30 deg plus trim and Magnus-coupling asymmetry -- approaching or crossing the 36.3 deg hull edge, after which C_M_pa/C_N_pa are linear extrapolations of sign-changing data rather than measurements; the resulting arc and every deploy seed for that LUT row are silently driven by fabricated Magnus moments (the canonical operating point peaks near ~14 deg, alpha^2 ~ 200, safely inside). A clamped/nearest extrapolation or an assert on the query range would make the edge visible.

*Verification:* 1 lens(es): REAL

## 11. [LOW] analysis.m:284 — robustness

**On the new fixed 0.1 s grid, test_points = 1:10:rows can select the appended ground-impact event row itself (altitude 0, full ~60 m/s impact velocity) as an envelope test point whenever rows is congruent to 1 mod 10, and in any case the last tested deploy now sits 0-0.9 s before ground impact.**

*Evidence:* analysis.m:284 `test_points = 1:10:size(ballistic_solution.trajectory, 1);` with eom2.m:73 `tspan = 0:0.1:t_max;` and the terminal impact event appended as the final row (eom2.m:82 Events + 196-200). rows = floor(te/0.1) + 2, so e.g. te in [19.9, 20.0) gives rows = 201 and 1:10:201 selects row 201 = the impact row (altitude 0, impact velocity, nose-down) as test point 21. CLAUDE.md's measured '21 test points' bounds te in [19.9, 20.9) for the canonical launch, so the final test point is at most 0.9 s (~55 m altitude) and possibly 0 s above ground impact -- vs the old 197-row natural grid [...]

*Failure scenario:* Canonical envelope run with impact time in [19.9, 20.0) (or any future launch tuning that lands rows on 1 mod 10): the loop seeds a drone AT the ground-impact point descending ~60 m/s; the plant has no ground plane so it dives through z < 0 and can still satisfy ballistic_success (final XY within 10 m; the criterion never checks z), silently adding a physically-impossible marker at the impact point in fig 39 and shifting the envelope success fractions quoted in the paper.

*Verification:* 1 lens(es): REAL

## 12. [LOW] analysis.m:322 — robustness

**The rotvelout shape guard `if size(rot,2) ~= 3, rot = rot.'; end` mis-orients the [3x1xT] (lqr/dsmc) trace when T == 3 exactly: squeeze yields a 3x3 coord-by-time matrix, the transpose is skipped, ballistic_success's row-count assert passes silently, and the rotation diagnostics for that degenerate trial are computed from transposed data.**

*Evidence:* analysis.m:321-322 `rot_lqr = squeeze(lqr_trial.rotvelout.Data); if size(rot_lqr, 2) ~= 3, rot_lqr = rot_lqr.'; end` (same pattern through line 330 and in sweep_landing_centroid.m's extraction). For [3x1x3] data, squeeze gives a 3x3 matrix whose rows are coordinates; size(.,2) == 3 skips the transpose; ballistic_success's grid assert (size(RotVel,1) == numel(t) -> 3 == 3) passes, and vecnorm(RotVel,2,2) then norms per-coordinate-across-time rows instead of per-sample rate vectors. The pid/smc [Tx3] shape is handled correctly for every T including 3.

*Failure scenario:* A trial whose 1e5 blowup guard fires after exactly 3 logged samples (t ~ 0.04 s): max_rot/max_rot_post in the info struct (stored as results(...).criteria in the sweep) are computed from transposed garbage. The success flag itself cannot flip -- 3 samples implies an early stop, so the duration clause already fails -- but any future consumer that reads the rotation extrema from criteria (or reuses this extraction where success is not duration-gated) inherits wrong numbers; effectively unreachable today, a real trap on reuse.

*Verification:* 1 lens(es): REAL

## 13. [LOW] docs/BALLISTICS_REFERENCE.md:50 — doc-mismatch

**BALLISTICS_REFERENCE.md still documents the removed 15-state ODE with integrated Euler-angle states 13-15, contradicting the 2026-07-10 12-state remediation.**

*Evidence:* docs/BALLISTICS_REFERENCE.md:50 '15-state ODE function. State partitioning:' with table row (line 58) '| 13-15 | o | Euler angles (rad) |' and line 34 'Propagates 15-state ODE via ode45()'. Current eom2.m integrates 12 states (x0 = [v0; h0; r0; e0] at line 64; removal comment at lines 56-62) and backfills trajectory cols 13:15 post-hoc with the derived pointing-vector attitude (lines 140-148). CLAUDE.md documents the new design correctly; this designated subsystem reference doc was not updated.

*Failure scenario:* A developer following the reference doc treats trajectory cols 13:15 as solver-integrated Euler states (or assumes a 15-state x0/ODE when modifying eom2 or adding events indexed past state 12), reintroducing exactly the interpolate-the-angle-columns / state-count mismatches the plan-2 remediation eliminated.

*Verification:* 1 lens(es): REAL

## 14. [LOW] Ballistics_Simulation-master/aero_constants.m:8 — doc-mismatch

**aero_constants requires the Aerospace Toolbox (gravitywgs84) but CLAUDE.md's required-toolboxes list omits it, so a machine provisioned per the docs fails at the first ballistic call.**

*Evidence:* aero_constants.m:8: `env.gravity = gravitywgs84(env.alt, env.L);` -- shipped only in Aerospace Toolbox (confirmed at /Applications/MATLAB_R2025a.app/toolbox/aero/aero/gravitywgs84.m). CLAUDE.md's 'Required toolboxes' line lists Simulink, Control System, Navigation, Optimization, Statistics and Machine Learning, and Symbolic Math -- no Aerospace Toolbox.

*Failure scenario:* The next consumer sets up a license/machine with exactly the documented toolboxes and runs any driver (analysis.m, trajectory_optimization.m, j3_lut_regen.m, Mortar_Sim.m): the aero_constants call (sweep_landing_centroid.m:78 / analysis.m:266) dies with 'Unrecognized function or variable gravitywgs84' before any simulation starts. One-line doc fix (or replace with a hard-coded g given env.alt = 0, env.L = 0).

*Verification:* 1 lens(es): REAL

## 15. [LOW] sweep_landing_centroid.m:92 — doc-mismatch

**Stale comments in both eom2 consumers still describe trajectory cols 13:15 as 'the invalid legacy Euler channel (deg/rad-mixed, NWU-mangled)', contradicting the same-day plan-2 remediation under which eom2 backfills those columns with the valid derived attitude.**

*Evidence:* sweep_landing_centroid.m:90-93: `% ... cols 13:15 are the invalid legacy Euler channel (deg/rad-mixed, NWU-mangled) and are deliberately not consumed -- see ballistic_deploy_state.` Twin at analysis.m:248-250: `% ... (2026-07-10 fix; cols 13:15 are the invalid legacy Euler channel -- see ballistic_deploy_state.m).` Both contradict the current contract: eom2.m:140-148 backfills cols 13:15 with the derived [theta phi psi] ('at any trajectory row ballistic_deploy_state(...) returns xi(7:9) equal to these columns exactly') and ballistic_deploy_state.m:10-16 documents the same ('trajectory [...]

*Failure scenario:* A developer implementing the planned schema-extended LUT regen (or auditing the seeding chain) reads either comment, concludes eom2 still emits the deg/rad-mixed mangled channel, and either re-reports the already-fixed B4 defect, 'fixes' the presumed-broken backfill, adds a redundant attitude re-derivation, or documents the columns as unusable -- diverging from the canonical contract; behavior-neutral today since the code correctly interpolates r and calls the helper.

*Verification:* 1 lens(es): REAL

---

## Refuted (1)
- `Ballistics_Simulation-master/eom2.m:233` — cos_taoa = dot(v,r)/|v| is unclamped (and not divided by norm(r)) while the ODE never renormalizes r, so integration drift |r| > 1 during a near-nose-on pass makes acos return a complex alpha that crashes the scatteredInterpolant Magnus lookups mid-integration.
  - why: Quoted code verified verbatim (eom2.m:233-234 unclamped acos of (v·r)/v_mag; raw r=x(7:9) at line 220, dr=h×r at 326-328 with no renormalization; scatteredInterpolant queries at 245/249 per aero_const

## Residual gap (CLOSED — see Addendum)
- analysis.m L~500-2440: apogee-run figure/score consumers (figs 1, 36, 37, 38) — the only ballistic-dependent code region no finder audited
- The gap finder for this region failed on a session usage limit during the original run; the resumed run (52/52 agents, +753k tokens, 19 min) completed it: **4 additional CONFIRMED findings (1 high)** — see the Addendum.

## Completeness critic
The 7-finder sweep covered the reworked ballistic chain thoroughly and its self-reported coverage claims check out against the repo. File inventory: every file in 'Ballistics_Simulation-master/' was read (eom2.m, aero_constants.m, Mortar_Sim.m, README.md, std_atm.csv, the xlsx; images/ contains no code), and all chain drivers, helpers, replot scripts, and both log-schema round-trips (direct MAT5 parsing by the contracts finder) were audited. I independently closed the remaining coverage candidates rather than reporting them as gaps: (1) dsmc_constraints.m / dsmc_no_constraints.m were read by NO finder despite 227/4 changed lines vs HEAD da9f96e, but a comment-stripped diff proves the entire uncommitted drift on every chain-adjacent modified .m file is comment-only (three trailing-comment additions on unchanged code lines repo-wide), and Omega2_max = 2 (canonical) at dsmc_constraints.m:98 — the controller the regeneration will fly is code-identical to the published, separately-audited state; (2) logs/ballistic_log.mat has zero consumers (only Mortar_Sim.m writes it); (3) j3_surrogate_refit.m:190 and j3_lut_regen.m:131 both pass visualize=false explicitly and sweep_landing_centroid defaults false on nargin<2, so no TO-log clobber path exists; (4) new files ballistic_deploy_state.m / j3_lut_regen.m / j3_surrogate_refit.m are untracked as expected. Exactly one genuine unowned region remains: the apogee-run consumer block of analysis.m (~L500-2440). The analysis-ballistic finder scoped L225-360 + L2440-2580, and both it and the success-criterion finder explicitly labeled the four 100-s apogee sims 'figure-only' without auditing the figure/score code they feed (fig 1 subplots 17-20, fig 36 kinetic energy, fig 37 KE score matrix, fig 38 'settling time, ballistic' panel — all paper-facing and regenerated in the imminent campaign). I confirmed concrete hazards in that span (interp1 NaN-fill past an early blowup-guard stop with no omitnan downstream; settling measured against the trajectory's own final position rather than the commanded xf_ballistic; a dead identically-zero self-distance block), so it warrants one finder pass. Two borderline misses noted for the coverage report but not gap-worthy: CLAUDE.md's sweep bullet says the criterion checks 'final XY within 10 m of the deploy point' while the verified code targets the commanded landing-lattice point (the doc-audit lens was applied elsewhere, this sentence slipped); and the eom2-physics finder's de(2) earth-curvature note (~1.3 mm effect) was deliberately left below the reporting bar, which I concur with.

## Finder coverage (self-reported, spot-checked by critic)
### eom2-physics
SCOPE: eom2.m physics/dynamics (ODE RHS, ICs, aero tables, frames, units). METHOD: full read of eom2.m + aero_constants.m + Mortar_Sim.m + BALLISTICS_REFERENCE.md; extracted all coefficient tables from Aerodynamic_Char_120mm_Mortar.xlsx (zip/XML) and std_atm.csv; transcribed the complete RHS+ICs into pure-Python RK4 and validated the replica against the shipped figs/0_ballistic_trajectory.png (apogee ~391 m, impact ~(867,377) m, 20.1 s) and the documented apogee h.r=-0.807 (replica: -0.8070) before quantifying deltas. No MATLAB launched. CHECKED CLEAN: (1) ICs -- v0/r0 are exactly McCoy's Ch.9 parametrization (unit-norm; el applied in vertical plane, az out-of-plane -- internally consistent across v0/r0/dx0/Q); dx0 verified algebraically orthogonal to r0 (so h0 x r0 = dx0 holds, incl. p!=0); h0=(I_x*p/I_y)r0+cross(r0,dx0) matches McCoy 9.4; spin recovery p=(I_y/I_x)(h.r) at line 258 correct. (2) dh equation -- all five moment terms (spin damping C_lp, fin cant C_ldelta, overturning C_Ma x (v x r), Magnus moment C_Mpa x (v-(v.r)r), pitch damping CMq x (h-(h.r)r)) dimensionally correct, correctly /I_y for h=H/I_y, with signs consistent with extracted table data (C_Ma0<0 statically stable, CMq+CMa<0 damping, C_lp<0 spin decay; replica reproduces the documented h.r decay -0.8438 -> -0.807 by apogee). (3) dr=h x r correct (transverse h = transverse omega). (4) Gravity [...]

### eom2-numerics
Scope (a) 12-state packing: verified every state index post o-channel removal - x0=[v0;h0;r0;e0] (eom2.m:64), ODE unpack v=x(1:3)/h=x(4:6)/r=x(7:9)/e=x(10:12) (218-221), event value y(11)=altitude (197), apogee max(x(:,11)) (90), impact interp over x(I:end,11) (100-104, 131), plots use cols 10/11/12 only; zero leftover references to cols 13:15 or the removed o channel anywhere in the ODE/event/output path; supertrans is exactly 4 blkdiag blocks = 12x12 matching the state width (a 15-col leftover would error dimensionally). NUEtoNWU = [1 0 0;0 0 -1;0 1 0] verified correct (n->n, w=-e, u->u) and proper (det=+1), so rotating the pseudo-vector h componentwise is valid. Backfill block: r renormalized via vecnorm before angles, asin clamped, atan2 scale-invariant, column order [theta_b, zeros, psi_b] = [theta phi psi] matching the contract; operation-by-operation identical to ballistic_deploy_state.m (which also renormalizes), so the exact-equality claim at eom2.m:140-144 holds. (b) tspan/event: resolved against the installed R2025a sources (no MATLAB launched). ode45.m lines 445-467 (RequestedPoints branch): the terminal event time is appended exactly once via the stop branch; a DUPLICATE final row (identical t and y) occurs only if the regula-falsi event time bit-exactly equals a 0:0.1:t_max grid point - measure-zero for a physical impact (it would then produce a zero-length [...]

### deploy-state
Scope: deploy-seed frame contract (ballistic_deploy_state.m vs system_dynamics.m, both call sites, edge cases). Static review only; algebra verified with bare python3 (no numpy/sympy on this machine) via 300-sample random-angle identity checks and finite-difference propagation of the plant's own Euler-rate rows. CLEAN ITEMS: (a) hand-built M (helper lines 48-50) is entry-for-entry IDENTICAL to the coefficient matrix of system_dynamics.m xdot(10:12) (exact 0.0 difference; rows 1:2 = standard ZYX body->world, row 3 negated); M*M.' = I to 4e-16; det(M) = -1 to 4e-16; M = diag(1,1,-1)*R_zyx exactly; M(:,1) = r_hat to 1e-15 under psi=atan2(r2,r1)/theta=asin(r3)/phi=0, including the vertical-r edge (atan2(0,0)=0 still gives M(:,1)=[0;0;+-1]). (b) M.' is a correct world->body map for PROPER vectors: velocity seeding reproduces world velocity exactly (M*M.'*v = v), and the plant's translational dynamics in the NWU world are physically consistent (gravity -z, thrust along -M(:,3)), so vel/pos seeding is exact. (c) TRACED, DEFECT FOUND (finding 1): the plant's slots x(4:6) are the body rates of the standard right-handed ZYX plant, but because the model's body->world map is improper (det -1), the observable world rotation of the body triad is -M*s, not +M*s (finite-diff residual 3.6e-9 vs O(1) for +M*s); the helper's rotvel0 = M.'*h therefore realizes world tumble -h, nose rate [...]

### sweep
Scope file read in full (705 lines); every audit item (a)-(i) exercised, plus numeric spot-checks in python (no MATLAB launched). CLEAN results by item: (a) eom2 call L80 -- eom2 reads launch fields by name (eom2.m:5-16) and uses isfield only for optional rel_tol/abs_tol (L80-81), so extra fields (saturation_on, label) are tolerated; required fields are not asserted but both drivers pass the complete 12-field set (trajectory_optimization.m:11-23; j3_lut_regen.m:124-128 with all 6 LHS dims + 6 fixed fields), and a missing field errors immediately with a clear 'Unrecognized field name'. (b) Arc stations L84-97 -- arc_3d strictly increasing unless two trajectory rows are bit-identical; with the fixed 0:0.1:t_max tspan plus appended terminal-event row, a duplicate requires the event time to coincide bit-exactly with a grid point (shell speed >= ~40 m/s everywhere, so no zero-motion segments; assessed measure-zero, not reported); deploy_arc spans 20-80% of arc so interp1 never extrapolates; the interpolated pointing vector deploy_r is consumed ONLY by ballistic_deploy_state (L174-175), which renormalizes (helper L39, 'linear interpolation shrinks unit vectors') -- nothing else touches raw deploy_r. (c) ct_dir L101-113 -- [-t_y;t_x] is CCW-left in right-handed NWU (checked: north travel -> west = left); fallback uses final-row ground-track direction, valid because both drivers [...]

### analysis-ballistic
SCOPE COVERED: analysis.m L225-360 (launch structs, both eom2 calls, apogee test, envelope loop, per-arm scoring, mask recording) and L2440-2580 (fig 39), plus referenced files (trajectory_optimization.m, ballistic_deploy_state.m, ballistic_success.m, sweep_landing_centroid.m, eom2.m, system_dynamics.m, constants.m, Mortar_Sim.m launch struct, CLAUDE.md contract sections). VERIFIED CLEAN: (a) Launch structs at analysis.m:226-238 and :268-280 are identical to each other and to trajectory_optimization.m base_params (Vo 100, el 45, az 15, w_z0 1, w_y0 0.5, p -8.379 present at BOTH sites, alpha_0 2, beta_0 -0.5, zeros, t_max 300); complete field set for eom2 (which reads exactly those 12 + optional rel_tol/abs_tol, defaulting to the tightened 1e-8/1e-10 at eom2.m:80-81); Mortar_Sim.m's standalone launch struct is also complete (p=0 there is the documented standalone default, spin only claimed for the three drone-facing launches). (b) Apogee test wiring correct: deploy_point = trajectory(apogee_idx,:) is an exact grid row; helper xi(10:12) = pos_nwu so xf_ballistic = [xi(10:11);0] is the deploy world XY at ground; NO 15-element padding exists (see finding on CLAUDE.md:93 - all drivers supply 12-element xi); base-workspace vars for the normal-mode single sims (A,B nominal, constants_struct+bus, dt, Kp_d/Ki_d, x0_step from L181, xf from L200, xi, xf_ballistic, simcase) are all [...]

### success-criterion
Audited ballistic_success.m (all 124 lines) and all call sites. Grep confirms exactly six calls repo-wide: sweep_landing_centroid.m L251 and analysis.m L331/334/337/340/343 (j3_lut_regen.m and surrogate_optimize.m mention it in comments only; the apogee 100-s runs at analysis.m L255-263 are figure-only and never scored, matching CLAUDE.md 'envelope, all five arms'). ZERO defects found; per-item verification: (a) OPTS: arguments block; defaults ReachTol=10, VelRatioMax=2, RotRatioMax=2, RotRefNorm=norm([pi;pi;pi])=5.4414, RotGraceT=1 (mustBeNonnegative), RotVel=[] - all name-value overridable, exactly per contract. (b) DURATION (L71): t(end) >= expected_T - 1e-6. Simulink lands the final major step exactly on StopTime (30 and 60 are exact multiples of the 0.02 s rate), so nominal runs give t(end)==expected_T exactly; a blowup-guard stop shaves >= one 0.02 s step (verified 59.98 >= 60-1e-6 is false), so the 1e-6 tolerance is neither falsely failing nor passing real early stops; a guard trip exactly AT the final sample passes duration but the 1e5 norm is then caught by the velocity clause (1e5/v0 >= 1000 with v0 <= ~100) or the rotation clause (1e5/5.44 post-grace). Empty t would error at t(end) but is unreachable: the sweep skips trials with non-empty ErrorMessage before scoring (L225-229), and analysis.m's plain sim() rethrows model errors before any scoring. (c) FINAL-XY [...]

### contracts
Cross-file contract audit of the ballistic chain; static only (no MATLAB). Checked clean unless listed as a finding. (a) trajectory-column consumers: grepped 'trajectory(' across all tracked .m (rg-gitignore-aware limitation worked around with explicit paths into Ballistics_Simulation-master/ and deprecated/); every live slice is cols 1:3/4:6/7:9/10:12, (:,10:11), (end,10:11), or (:,10)/(:,11)/(:,12) plotting -- matches the 15-col NWU contract; NO live consumer of cols 13:15; git diff HEAD confirms the old deg2rad(deploy_point(13:15))/eul2rotm/rot0 seeding is fully removed from analysis.m (both sites) and that the old xi was also 12-element, so the helper introduces no xi-length interface change for the four *_single models; no eul2rotm or stray deploy_rot anywhere; deprecated/ has no ballistic consumers. (b) eom2 call sites: Mortar_Sim.m, analysis.m x2, sweep_landing_centroid.m via traj_params from trajectory_optimization.m / j3_lut_regen.m LHS loop / j3_surrogate_refit.m verify path -- all 12 required launch fields present in every path (deg for el/az/alpha_0/beta_0, rad/s for w_z0/w_y0/p); extra fields (saturation_on, label) are tolerated (eom2 reads named fields only); nobody passes rel_tol/abs_tol (tightened defaults 1e-8/1e-10 apply, as intended); sat-on defaulting is homogeneous across LUT training rows, the injected reference row, and the refit verify sweep (no [...]

---

## Recommended fix order (author gates marked)

1. **[GATE — physics] eom2.m:261 lift**: drop the extra `v_mag` (`C_tilde_L_a = rho*S*C_L_a/(2*m)`), per McCoy eq. 9.1 and dimensional homogeneity. Expected canonical-arc shift: apogee 391 -> ~234 m, flight 20.1 -> ~13.9 s, impact (867,377) -> ~(909,348).
2. **[GATE — physics] eom2.m:262 Magnus force**: restore `S` (`rho*S*d*C_N_pa*p/(2*m)`); currently ~1/S ≈ 89x too strong, ~2-4 m spin-correlated displacement.
3. **[GATE — physics] eom2.m:282-284 Coriolis lambda**: degree-valued `az_0` (and latent `env.L`) inside radian `sin/cos`, plus firing-azimuth double-count in the north-fixed frame (v0/r0 already carry az). Decide the correct McCoy frame convention; ~0.5-0.7 m bias today, az-correlated across the LUT box.
4. **[GATE — seeding] ballistic_deploy_state.m:53**: `rotvel0 = -(M.'*h_nwu)` — angular velocity is a pseudovector; through the improper (det = -1) body-to-world map the realized world spin of the coded seed is `-h` (mirror tumble). Velocity/position/attitude seeds are unaffected (proper vectors). Decide jointly with the h-vs-omega axial question (finding 7): the seed axial rate is `(I_x/I_y)*p ≈ -0.807`, the physical rate is `p = -8.379`.
5. j3_lut_regen.m:76 checkpoint fingerprint: add a ballistics/criterion config hash (and delete the stale `logs/centroid_lookup_ckpt_j3.mat`) so a resume can never re-stamp pre-fix data as canonical.
6. sweep_landing_centroid.m:38: replace `pgrep -c` (BSD pgrep has no `-c`; guard is dead) with e.g. `pgrep -x MATLAB_maca64 | wc -l`.
7. Doc/comment hygiene batch: CLAUDE.md xi 15->12 and the h=omega claim (both corrected 2026-07-10); BALLISTICS_REFERENCE.md 15-state ODE section; stale "cols 13:15 mangled" comments in sweep_landing_centroid.m:92 + analysis.m; eom2 .alpha/.beta naming vs true AoA; aero_constants Magnus-hull extrapolation clamp/assert + Aerospace Toolbox (gravitywgs84) in the toolbox list; envelope impact-row test point (analysis.m:284); T==3 rotvel shape-guard trap (analysis.m:322).
8. Then: re-run the B4-style verification chain (unit checks, operating-point sweep) on the corrected physics, and only then launch the schema-extended regeneration (LUT + sat sweeps + envelope). Note the corrected arc is ~6 s shorter and ~160 m lower — station geometry, arc length, and the W2 hand-off-window prior all change.

## Provenance
- Workflow run `wf_a29859aa-4d4` (session 946e5f6e); journal: `subagents/workflows/wf_a29859aa-4d4/journal.jsonl`.
- Result JSON: `/private/tmp/.../tasks/wmdaofe32.output`; digest: job tmp `bugsweep_digest.txt`.
- Sweep executed the same day as (and after) the deploy-attitude fix, plan-2 eom2 remediation, and tolerance tightening — findings 1-4 invalidate the *numbers* from those verifications (the code-level fixes remain valid), including the provisional operating point 0.300.

---

## Addendum — gap round (resumed run, same run ID `wf_a29859aa-4d4`)

Scope: analysis.m L~500-2440 apogee-run figure/score consumers (figs 1, 36, 37, 38) — the one region the original seven finders did not own. 43 cached agents replayed; the gap finder + verification panels ran live (52/52 done, 0 errors). Four findings, all confirmed:

### 16. [HIGH] analysis.m:2325 — criterion-scoring

**Fig 38's 'Relative % 95% settling time, ballistic' settles each run toward its own final interpolated position instead of the commanded xf_ballistic target, so a diverged run is mathematically guaranteed a finite, competitive-looking settling time -- already realized in the on-disk fig 38, which scores the diverged LQR apogee run within 3% of the converged cSMC/dSMC.**

*Evidence:* L2325 `xf_lqr_ballistic = interp_step_lqr_ballistic_pos(end,:,:);` (own t=100 position, not the commanded L253 `xf_ballistic = [xi(10:11,:);0]`); L2331 `delta_lqr = vecnorm(interp_step_lqr_ballistic_pos(i,:,:) - xf_lqr_ballistic,2,3);`; L2341 normalizes by `norm(...(xf_lqr_ballistic ... - xi_lqr_ballistic ...))`; L2346-2347 latch `if (delta_lqr <= 0.05 && isnan(ts_xf_lqr_ballistic)) ts_xf_lqr_ballistic = ballistic_time(i);`. For pos ~ c*t^k the ratio ||p(t)-p(T)||/||p(T)-p(0)|| crosses 0.05 at t = 95.0 s (k=1), 97.5 s (k=2), 99.5 s (exponential 0.1/s) -- always finite. Observational proof from the 2026-07-03 artifacts: figs/36_kinetic_energy.png 'Ballistic, LQR' panel shows KE growing monotonically to ~1.65e7 J at t=100 (||v|| = sqrt(2*1.65e7/0.8) ~ 6.4 km/s, under the 1e5 guard so the run completes and stays NaN-free); figs/37 ballistic panel prints 100.00% for LQR vs all (avg-KE ratio >= 4e4); yet figs/38 bottom panel prints finite LQR cells of 3.0%/1.8%/18.3% vs cSMC/dSMC/PID.

*Failure scenario:* Last full run (2026-07-03, figures on disk): the LQR 100-s apogee run never arrests the tumble and flies away at ~6.4 km/s, ending on the order of 10^2 km from the commanded deploy-XY target; the criterion measures approach to that runaway endpoint, reports ts_lqr ~ 95-97 s, and fig 38 presents the diverged controller as settling within 3% of the controllers that actually landed and held. The pending post-deploy-fix regeneration will re-emit the same bogus panel for any baseline that drifts or diverges without tripping the 1e5 guard.

*Verification:* 3 lens(es): REAL, REAL, REAL
  - lens 1 (high): Confirmed at every level. (1) Code is verbatim as quoted: analysis.m L2325-2328 set the ballistic settling "target" to each run's own final interpolated position (`interp_step_*_ballistic_pos(end,:,:)`), never referencing the commanded `xf_ballistic = [xi(10:11,:);0]` (L253); L2331 measures distance to that endpoint, L2341 normalizes by the run's own start-to-end displacement, L2346-2357 latch the first ratio <= 0.05. (2) The finite-ts [...]
  - lens 2 (high): Documentation audit: no doc records the endpoint-referenced settling convention as a deliberate decision, and none acknowledges its diverged-run implication. CLAUDE.md describes figs 35-38 only as pairwise score matrices with the score formula (silent on the settling reference). The in-file comment (analysis.m L2262-2263, 'deviation from xf <= 5% of its step |xf - xi|') and docs/README.md 'Settling Time' section ('maximum positional error [...]
  - lens 3 (medium): Finding verified end-to-end and reachable — it is realized in the committed artifacts, not hypothetical. Code: analysis.m:2325 sets xf_lqr_ballistic to the run's own final interpolated position (interp onto ballistic_time = 0:0.1:100, L647/L739); the commanded xf_ballistic (L253) is never used in the settling loop (L2330-2358), and since distance-to-own-endpoint is exactly 0 at the last sample, the 5% first-crossing latch (L2346-2347) [...]

### 17. [MEDIUM] analysis.m:739 — robustness

**If any of the four unscored 100-s apogee runs trips the in-model 1e5 blowup guard before StopTime, interp1's default no-extrapolation NaN tail silently propagates through the omitnan-less KE means and the settling xf into blank MissingData rows/columns of the fig 37 and fig 38 heatmaps, with no error or warning anywhere.**

*Evidence:* L739-746 `interp_step_lqr_ballistic_pos = interp1(step_out_lqr_ballistic.posout.Time, permute(...,[3 1 2]), ballistic_time);` with `ballistic_time = 0:0.1:100` (L647) -- default linear interp1 returns NaN for queries past Time(end). L2184 `avg_step_lqr_ballistic_ke = mean(mean(step_lqr_ballistic_ke, 2));` (no omitnan; grep confirms zero omitnan/rmmissing in the file) -> NaN average -> L2216 `ke_scores_ballistic(i,j) = 100*(...)/(...)` NaN in that controller's full row+column -> L2250 heatmap renders them as blank gray MissingData cells. L2325 `xf_lqr_ballistic = interp_step_lqr_ballistic_pos(end,:,:);` = NaN -> every L2331 delta NaN -> `NaN <= 0.05` false -> ts stays NaN -> fig 38 row/column blank too; fig 36 traces (L2135-2136) silently truncate. Nothing asserts posout.Time(end)==100.

*Failure scenario:* Post-deploy-attitude-fix regeneration: PID (or LQR/SMC) at the new apogee seed hits the Euler-rate kinematic blowup -- post-fix 60-s sweeps already measured rotvel norms ~2e4, within 5x of the 1e5 guard, and these 100-s runs are longer and were never full-duration-verified -- the Stop chart fires at e.g. t=40 s; ballistic_time samples 401..1001 become NaN; avg_step_pid_ballistic_ke = NaN; figs 37 and 38 regenerate with 7 blank cells each in the ballistic panels and the campaign publishes/archives them without any indication a sim terminated early.

*Verification:* 3 lens(es): REAL, REAL, REAL

### 18. [LOW] analysis.m:2162 — robustness

**The fig 36 'Ballistic, PID' subplot's y-limit is slaved to 5x the max of LQR's ballistic KE, so the currently-diverged LQR run (max ~1.65e7 J) sets the PID axis to [0, ~8.3e7] and renders PID's O(10 J) trace as an unreadable flat line -- realized in the on-disk figure.**

*Evidence:* L2162 `ylim([0, max(max(step_lqr_ballistic_ke)*5)])` inside the subplot(5,4,19) PID block (L2154-2163), mirroring the spiral idiom at L2115. figs/36_kinetic_energy.png on disk shows the 'Ballistic, PID' panel with a x10^7 axis and an invisible trace hugging zero, while fig 37's cross-scores (PID ~ 0.6x cSMC's O(50-100 J) average) prove the hidden PID trace is small and well-behaved.

*Failure scenario:* Any run where LQR diverges (the current state and plausibly the post-fix regen) produces a fig 36 PID ballistic panel that conveys zero information -- a reviewer comparing controller KE traces sees an apparently empty plot and cannot distinguish PID converging at tens of joules from PID at zero, while the same figure's LQR panel autoscales and displays fine.

*Verification:* 1 lens(es): REAL

### 19. [LOW] analysis.m:760 — robustness

**The four ballistic pairwise-distance blocks (L757-823) compute identically-zero 1x10 self-distance rows -- the single drone's trajectory broadcast against 10 repmat copies of itself -- and repo-wide grep shows zero consumers, making 67 lines of dead code whose meaningless all-zero outputs are persisted into logs/analysis_log.mat.**

*Evidence:* L760-763 `for i=1:1 / temp = repmat(interp_step_lqr_ballistic_pos(:, i, :), [1, 10, 1]); / temp_pos = abs(interp_step_lqr_ballistic_pos - temp); / temp = sqrt(sum(temp_pos.^2, 3));` -- the T x 1 x 3 single-drone array minus 10 copies of its own column is exactly zero, so min/avg/max (L764-771) are 1x10 zeros; same for the smc/dsmc/pid clones (L774-823). `grep -rn 'ballistic_min_distances|ballistic_avg_distances|ballistic_max_distances'` over all repo .m files returns only the L757-823 assignment lines, whereas the step/uncertainty analogues ARE consumed (heatmaps L1098-1110/L1485-1497, fig-35 Frobenius scores L1851-1853). L2575 `save("logs/analysis_log.mat")` persists the zeros.

*Failure scenario:* A future figure or score wired to step_*_ballistic_{min,avg,max}_distances (by analogy with the consumed step/uncertainty matrices, e.g. a 'swarm spread, ballistic' heatmap) silently reads identically-zero data and reports perfect formation-keeping for a single-drone case where the quantity is undefined; meanwhile every regen burns cycles computing and archiving the zeros.

*Verification:* 1 lens(es): REAL

### Fix-order additions (append to the numbered list above)

9. **[metric semantics — author-visible] analysis.m:2325**: fig 38 ballistic settling must measure approach to the commanded `xf_ballistic` (L253), not each run's own final position; as shipped, a diverged run is guaranteed a finite, competitive settling time (realized on disk: diverged LQR within 3% of converged cSMC/dSMC).
10. analysis.m:739 (and the smc/dsmc/pid clones): assert the apogee runs reach StopTime (or use omitnan + an explicit early-stop annotation) so a tripped 1e5 guard cannot silently blank fig 37/38 rows via the interp1 NaN tail.
11. analysis.m:2162: let the fig 36 PID ballistic panel autoscale (or slave it to its own trace), not 5x LQR's max.
12. analysis.m:757-823: delete the four dead single-drone self-distance blocks (identically-zero 1x10 rows, zero consumers, persisted into the log).

### Updated totals

**19 CONFIRMED** — 5 high (eom2 lift, eom2 Magnus-S, eom2 Coriolis, deploy mirror-tumble, fig 38 settling-to-own-endpoint), 6 medium, 8 low; 1 refuted; coverage now includes the full apogee-run figure chain. Combined provenance: 52 agents, ~4.77M subagent tokens, ~106 min total across both passes.
